interface Config {
  port: number;
  dirs: string[];
  keys: string[];
}

const config: Config = {
  port: 8080,
  dirs: ["suicidey", "pp", "kms", "fbi", "cia", "666", "777"],
  keys: ["cummingforjesus"],
};

export default config;
