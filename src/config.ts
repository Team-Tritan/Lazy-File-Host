interface Config {
  port: number;
  dirs: string[];
  keys: string[];
}

const config: Config = {
  port: 8080,
  dirs: ["suicidey", "cock-vids", "kms", "fbi", "cia", "bricked", "666", "777"],
  keys: ["fuckurmom"],
};

export default config;
