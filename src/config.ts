"use strict";

interface Config {
  port: number;
  dirs: string[];
}

const config: Config = {
  port: 8080,
  dirs: ["suicidey", "pp", "kms", "fbi", "cia", "666", "777"],
};

export default config;
